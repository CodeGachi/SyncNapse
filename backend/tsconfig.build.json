{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node", "multer"],
    "skipLibCheck": true
  },
  "exclude": [
    "node_modules",
    "dist",
    "**/*.spec.ts",
    "jest.config.ts"
  ]
}
