# SyncNapse AI 기능 강화 제안서

> AI 경진대회를 위한 연구 및 개선 가능한 핵심 AI 기능 제안

## 📊 현재 AI 기능 분석

### 현재 구현된 AI 기능

1. **RAG 기반 AI 챗봇**
   - Google Gemini API 사용
   - LlamaIndex를 통한 벡터 검색
   - PDF, 음성 전사, 노트 콘텐츠 통합
   - 질문/요약/퀴즈 생성 모드

2. **실시간 음성 인식 (STT)**
   - Web Speech API 사용 (브라우저 기반)
   - 한국어 음성 인식
   - 실시간 전사 (Transcription)

3. **PDF 텍스트 추출**
   - pdf-parse 라이브러리 사용
   - 페이지별 텍스트 추출

### 현재 시스템의 한계점

1. **음성 인식의 정확도 부족**
   - Web Speech API는 브라우저 의존적
   - 오프라인 불가능
   - 전문 용어/학술 용어 인식 정확도 낮음
   - 화자 분리(Speaker Diarization) 미지원

2. **RAG 시스템의 단순함**
   - 단순 벡터 검색만 사용
   - 컨텍스트 이해 부족
   - 다중 모달(Multi-modal) 미지원
   - 문서 구조 이해 부족

3. **OCR/이미지 분석 부재**
   - PDF의 이미지/차트/표 분석 불가
   - 손글씨 인식 미지원
   - 수식 인식 미지원

4. **개인화/적응형 학습 부재**
   - 사용자 학습 패턴 분석 미지원
   - 개인화된 요약/추천 부재
   - 학습 효과 측정 기능 없음

---

## 🎯 AI 경진대회를 위한 핵심 개선 제안

### 1. 고급 음성 인식 시스템 구축 ⭐⭐⭐

**연구 가치: 매우 높음**

#### 1.1 OpenAI Whisper 통합
```
현재: Web Speech API (브라우저 의존)
개선: Whisper 모델 (로컬/클라우드)
```

**구현 방안:**
- **Whisper Large-v3 모델 사용**
  - 다국어 지원 (100개 언어)
  - 높은 정확도
  - 타임스탬프 정확도 향상
  
- **실시간 처리 파이프라인**
  ```
  음성 스트림 → 청크 분할 → Whisper API → 실시간 전사 → DB 저장
  ```

- **연구 포인트:**
  - 실시간성 vs 정확도 트레이드오프 분석
  - 청크 크기 최적화 (예: 30초 윈도우)
  - 배치 처리 vs 스트리밍 처리 비교

**구현 파일:**
- `backend/src/modules/transcription/whisper.service.ts` (신규)
- `backend/src/modules/queue/processors/audio.processor.ts` (개선)

#### 1.2 화자 분리 (Speaker Diarization)
```
목표: "누가 말했는지" 자동 인식
```

**구현 방안:**
- **pyannote.audio 통합**
  - 화자 분리 모델
  - 타임스탬프와 화자 매핑
  
- **활용 시나리오:**
  - 교수/학생 발언 구분
  - 토론 수업 기록
  - 질의응답 자동 분류

**연구 포인트:**
- 화자 수 자동 감지
- 실시간 화자 분리 성능 최적화
- Whisper + Diarization 통합 파이프라인

#### 1.3 전문 용어 커스터마이징
```
목표: 학술/전문 용어 인식률 향상
```

**구현 방안:**
- **Fine-tuning Whisper**
  - 대학 강의 데이터셋 수집
  - 전공별 전문 용어 학습
  
- **용어 사전 (Vocabulary Boosting)**
  - 과목별 키워드 사전 구축
  - 후처리 단계에서 용어 보정

**연구 포인트:**
- Fine-tuning vs Vocabulary Boosting 효과 비교
- 전공별 정확도 향상률 측정
- 데이터 수집 방법론

---

### 2. 고급 RAG 시스템 구축 ⭐⭐⭐

**연구 가치: 매우 높음**

#### 2.1 하이브리드 검색 (Hybrid Search)
```
현재: 벡터 검색만 사용
개선: 벡터 + 키워드 + 그래프 검색
```

**구현 방안:**
- **벡터 검색 개선**
  - Embedding 모델 업그레이드 (text-embedding-004 → text-embedding-005)
  - Re-ranking 모델 추가 (Cohere Rerank, BGE-reranker)
  
- **키워드 검색 추가**
  - BM25 알고리즘
  - 전문 용어 정확 매칭
  
- **하이브리드 스코어링**
  ```
  final_score = α * vector_score + β * keyword_score + γ * recency_score
  ```

**연구 포인트:**
- 최적의 가중치(α, β, γ) 찾기
- 검색 품질 평가 지표 (MRR, NDCG)
- 쿼리 유형별 최적 전략

**구현 파일:**
- `backend/src/modules/ai/services/hybrid-search.service.ts` (신규)
- `backend/src/modules/ai/services/rag-engine.service.ts` (개선)

#### 2.2 지식 그래프 통합
```
목표: 문서 간 관계 파악 및 컨텍스트 확장
```

**구현 방안:**
- **엔티티 추출 및 관계 구축**
  - NER (Named Entity Recognition)
  - 개념 간 관계 추출
  
- **그래프 데이터베이스**
  - Neo4j 또는 PostgreSQL + AGE
  - 개념 네트워크 시각화
  
- **그래프 기반 RAG**
  ```
  사용자 질문 → 관련 엔티티 추출 → 그래프 탐색 → 확장된 컨텍스트 생성
  ```

**연구 포인트:**
- 자동 지식 그래프 구축 방법론
- 그래프 + 벡터 검색 통합 효과
- 교육 도메인에서의 효용성 평가

#### 2.3 Multi-hop Reasoning
```
목표: 여러 문서에 걸친 복잡한 추론
```

**구현 방안:**
- **Chain-of-Thought 프롬프팅**
  - 단계별 추론 과정 생성
  - 중간 결과 검증
  
- **Self-Consistency**
  - 여러 경로로 답 생성
  - 투표 또는 앙상블

**연구 포인트:**
- 복잡한 질문에 대한 정확도 향상률
- 추론 단계 수와 성능의 관계
- 계산 비용 vs 정확도 트레이드오프

#### 2.4 Query Understanding & Decomposition
```
목표: 복잡한 질문을 하위 질문으로 분해
```

**구현 방안:**
```python
질문: "머신러닝과 딥러닝의 차이점을 설명하고, 각각의 장단점을 비교하라"

분해:
1. 머신러닝이란 무엇인가?
2. 딥러닝이란 무엇인가?
3. 두 개념의 차이점은?
4. 머신러닝의 장단점
5. 딥러닝의 장단점
→ 각 답변 통합 → 최종 답변
```

**연구 포인트:**
- 질문 분해 전략
- 하위 답변 통합 방법
- 교육 질문 특성 분석

---

### 3. Multi-modal AI (이미지/표/차트 분석) ⭐⭐⭐

**연구 가치: 높음**

#### 3.1 OCR 및 구조화된 데이터 추출
```
목표: PDF의 표, 차트, 수식 이해
```

**구현 방안:**
- **고급 OCR**
  - Google Cloud Vision API
  - Azure Computer Vision
  - Tesseract (오픈소스)
  
- **수식 인식**
  - LaTeX OCR (Mathpix, Pix2Tex)
  - 수식 검색 가능하게 인덱싱
  
- **표 추출**
  - Table Transformer 모델
  - 구조화된 데이터로 변환

**연구 포인트:**
- OCR 정확도 vs 비용
- 수식 인식 정확도 향상
- 표 구조 이해 및 질의응답

**구현 파일:**
- `backend/src/modules/ai/services/ocr.service.ts` (신규)
- `backend/src/modules/ai/services/multimodal-rag.service.ts` (신규)

#### 3.2 Vision Language Model (VLM) 통합
```
목표: 이미지를 직접 이해하는 AI
```

**구현 방안:**
- **Gemini Vision API**
  - 이미지 + 텍스트 질의응답
  - 차트/그래프 해석
  
- **GPT-4V 또는 Claude 3.5 Vision**
  - 더 정교한 이미지 분석
  
- **활용 시나리오:**
  ```
  질문: "슬라이드 5페이지의 그래프가 보여주는 트렌드는?"
  → VLM이 그래프 이미지 분석 → 답변 생성
  ```

**연구 포인트:**
- VLM vs OCR+텍스트 모델 비교
- 비용 효율성
- 교육 콘텐츠 특화 프롬프팅

#### 3.3 손글씨 인식 (Handwriting Recognition)
```
목표: 학생이 그린 필기/스케치 인식
```

**구현 방안:**
- **Drawing Overlay 통합**
  - 현재 시스템의 Fabric.js 캔버스 활용
  - 손글씨 → 텍스트 변환
  
- **활용 시나리오:**
  - 학생 필기 검색
  - 손으로 쓴 질문 자동 인식
  - 수식 필기 → LaTeX 변환

**연구 포인트:**
- 실시간 손글씨 인식 성능
- 협업 환경에서의 활용도
- 다국어 손글씨 인식

---

### 4. 개인화 및 적응형 학습 시스템 ⭐⭐

**연구 가치: 높음**

#### 4.1 학습 패턴 분석
```
목표: 학생의 학습 스타일 파악
```

**구현 방안:**
- **사용자 행동 로깅**
  - 시청 시간
  - 반복 학습 구간
  - 질문 유형
  - 퀴즈 정답률
  
- **학습 프로필 생성**
  ```
  {
    "weakTopics": ["미분", "적분"],
    "learningPace": "slow",
    "preferredContentType": "visual",
    "activeHours": ["19:00-22:00"]
  }
  ```

**연구 포인트:**
- 학습 패턴 클러스터링
- 학습 효과 예측 모델
- 개인화 추천 알고리즘

**구현 파일:**
- `backend/src/modules/ai/services/learning-analytics.service.ts` (신규)
- `backend/src/modules/ai/services/personalization.service.ts` (신규)

#### 4.2 적응형 요약 (Adaptive Summarization)
```
목표: 사용자 수준에 맞는 요약
```

**구현 방안:**
- **난이도 조절**
  ```
  초급: 간단한 설명, 예시 중심
  중급: 핵심 개념, 원리 설명
  고급: 심화 내용, 수식 포함
  ```
  
- **관심 토픽 기반 요약**
  - 사용자가 자주 질문한 주제 강조
  - 약점 보완 콘텐츠 추천

**연구 포인트:**
- 사용자 수준 자동 판단
- 요약 만족도 평가
- A/B 테스팅

#### 4.3 지능형 퀴즈 생성
```
목표: 학습 효과를 극대화하는 퀴즈
```

**구현 방안:**
- **난이도 조절 퀴즈**
  - 이전 퀴즈 정답률 기반
  - Bloom's Taxonomy 적용
  
- **약점 집중 퀴즈**
  - 틀린 문제 유형 분석
  - 관련 개념 재학습 퀴즈
  
- **스페이스 리피티션 (Spaced Repetition)**
  - 망각 곡선 기반 복습 스케줄링
  - 효과적인 복습 타이밍 제안

**연구 포인트:**
- 퀴즈 난이도 자동 조절 알고리즘
- 학습 효과 측정 (사전/사후 평가)
- 최적 복습 주기 연구

---

### 5. 실시간 협업 AI 기능 ⭐⭐

**연구 가치: 중간**

#### 5.1 AI 토론 진행자 (Moderator)
```
목표: AI가 토론 수업을 진행
```

**구현 방안:**
- **토론 주제 추출**
  - 강의 내용에서 논쟁적 주제 발굴
  
- **토론 진행**
  ```
  1. 주제 제시
  2. 찬반 의견 수집 (Q&A 패널 활용)
  3. 반론 제시
  4. 결론 유도
  ```
  
- **발언 분석**
  - 주장-근거 구조 분석
  - 논리적 오류 지적

**연구 포인트:**
- 교육적 토론 진행 전략
- AI의 중립성 유지
- 학생 참여도 향상 효과

#### 5.2 실시간 질문 추천
```
목표: 학생들이 할 만한 질문 AI가 제안
```

**구현 방안:**
- **현재 컨텍스트 분석**
  - 현재 슬라이드 내용
  - 지금까지 나온 질문
  
- **질문 생성**
  ```
  "이 개념은 실제로 어디에 쓰이나요?"
  "이전 개념과의 차이점은 무엇인가요?"
  "예외 상황은 없나요?"
  ```

**연구 포인트:**
- 좋은 질문의 정의
- 질문 다양성 확보
- 학습 효과 측정

#### 5.3 자동 회의록 생성
```
목표: 협업 세션 자동 정리
```

**구현 방안:**
- **핵심 논의 사항 추출**
  - Q&A 내용
  - 투표 결과
  - 주요 의견
  
- **액션 아이템 추출**
  - 과제 도출
  - 후속 조치 정리

**연구 포인트:**
- 중요도 판단 기준
- 자동 정리의 완성도
- 사용자 만족도

---

### 6. 고급 프롬프트 엔지니어링 ⭐

**연구 가치: 중간**

#### 6.1 Few-shot Learning
```
목표: 예시를 통한 답변 품질 향상
```

**구현 방안:**
```python
프롬프트:
"""
다음은 좋은 답변 예시입니다:

질문: "경사하강법이란?"
답변: [예시 답변]

질문: "과적합이란?"
답변: [예시 답변]

이제 다음 질문에 답하세요:
질문: "{user_question}"
답변:
"""
```

**연구 포인트:**
- 최적의 예시 개수
- 예시 선택 전략
- 도메인별 효과

#### 6.2 Self-Reflection & Critique
```
목표: AI가 스스로 답변 검증
```

**구현 방안:**
```python
1단계: 답변 생성
2단계: 자체 비평 생성
  "이 답변의 문제점: ..."
3단계: 개선된 답변 생성
```

**연구 포인트:**
- 답변 품질 향상률
- 계산 비용 증가 대비 효과
- 환각(Hallucination) 감소 효과

---

## 🛠️ 구현 우선순위

### Phase 1: 즉시 구현 가능 (1-2개월)
1. **Whisper 통합** - 음성 인식 정확도 대폭 향상
2. **하이브리드 검색** - RAG 성능 개선
3. **Multi-modal RAG** - Gemini Vision 활용

### Phase 2: 중기 목표 (3-4개월)
4. **화자 분리** - 토론 수업 지원
5. **OCR 및 표 분석** - 자료 이해도 향상
6. **개인화 시스템 기초** - 학습 패턴 분석

### Phase 3: 장기 목표 (5-6개월)
7. **지식 그래프** - 개념 간 연결
8. **적응형 학습** - 완전한 개인화
9. **AI 토론 진행자** - 혁신적 협업

---

## 📈 평가 지표 (Metrics)

### 1. 음성 인식
- **WER (Word Error Rate)**: 단어 오류율
- **RTF (Real-Time Factor)**: 실시간성 지표
- **전문 용어 정확도**: 커스텀 테스트셋

### 2. RAG 시스템
- **MRR (Mean Reciprocal Rank)**: 검색 품질
- **NDCG (Normalized Discounted Cumulative Gain)**
- **답변 정확도**: 사람 평가 (1-5점)
- **Citation 정확도**: 출처 정확성

### 3. 학습 효과
- **학습 시간 단축율**: 이전 대비 시간 절약
- **이해도 향상**: 사전/사후 테스트
- **참여도**: 질문 수, 협업 빈도
- **만족도**: 사용자 설문 (NPS)

### 4. 시스템 성능
- **응답 시간**: P50, P95, P99
- **처리량 (Throughput)**: QPS (Queries Per Second)
- **비용**: API 호출당 비용

---

## 📚 연구 논문 주제 아이디어

### 1. "실시간 강의 환경에서의 하이브리드 검색 전략 비교 연구"
- 벡터, 키워드, 그래프 검색의 조합 효과
- 교육 도메인 특화 검색 전략

### 2. "Whisper 기반 실시간 화자 분리의 교육적 효용성 평가"
- 토론 수업에서의 화자 분리 정확도
- 학습 효과 측정

### 3. "Multi-modal RAG: 강의 자료의 이미지 및 표 분석 통합"
- VLM vs OCR+텍스트 모델 비교
- 교육 콘텐츠 이해도 향상

### 4. "적응형 학습 시스템: 학습 패턴 분석을 통한 개인화 전략"
- 학습 스타일 클러스터링
- 개인화 효과 측정

### 5. "AI 기반 협업 학습 플랫폼의 학습 효과 분석"
- 전통적 학습 vs AI 지원 학습 비교
- 장기적 학습 성과 추적

---

## 🎓 학습 및 참고 자료

### 음성 인식
- [OpenAI Whisper Paper](https://arxiv.org/abs/2212.04356)
- [pyannote.audio Documentation](https://github.com/pyannote/pyannote-audio)

### RAG 시스템
- [LlamaIndex Documentation](https://docs.llamaindex.ai/)
- [Advanced RAG Techniques](https://www.pinecone.io/learn/advanced-rag/)
- [Hybrid Search: BM25 + Vector](https://www.elastic.co/blog/hybrid-search)

### Multi-modal AI
- [Gemini Vision API](https://ai.google.dev/tutorials/python_quickstart)
- [GPT-4V System Card](https://openai.com/research/gpt-4v-system-card)
- [Table Transformer](https://arxiv.org/abs/2110.00061)

### 교육 AI
- [Bloom's Taxonomy](https://cft.vanderbilt.edu/guides-sub-pages/blooms-taxonomy/)
- [Spaced Repetition](https://gwern.net/spaced-repetition)

---

## 💡 실험 아이디어

### 실험 1: Whisper vs Web Speech API
```
목적: 음성 인식 정확도 비교
방법:
1. 동일한 10개 강의 녹음
2. 두 시스템으로 전사
3. WER 계산
4. 전문 용어 정확도 측정
```

### 실험 2: RAG 검색 전략 비교
```
목적: 최적의 검색 전략 찾기
방법:
1. 100개 질문 데이터셋 구축
2. 벡터만 vs 하이브리드 vs 그래프 비교
3. MRR, NDCG 측정
4. 사용자 만족도 조사
```

### 실험 3: 개인화 효과 측정
```
목적: 개인화가 학습에 미치는 영향
방법:
1. 두 그룹 (개인화 vs 일반)
2. 4주간 사용
3. 사전/사후 평가
4. 학습 시간, 이해도, 만족도 비교
```

---

## 🚀 빠른 시작: 첫 번째 개선 구현하기

### Step 1: Whisper API 통합 (추천)

```bash
# 1. OpenAI SDK 설치
cd backend
npm install openai

# 2. 환경 변수 추가
echo "OPENAI_API_KEY=sk-..." >> .env

# 3. Whisper 서비스 생성
```

```typescript
// backend/src/modules/transcription/whisper.service.ts
import { Injectable } from '@nestjs/common';
import OpenAI from 'openai';

@Injectable()
export class WhisperService {
  private openai: OpenAI;

  constructor() {
    this.openai = new OpenAI({
      apiKey: process.env.OPENAI_API_KEY,
    });
  }

  async transcribeAudio(audioBuffer: Buffer): Promise<string> {
    const response = await this.openai.audio.transcriptions.create({
      file: audioBuffer,
      model: 'whisper-1',
      language: 'ko',
      response_format: 'verbose_json',
      timestamp_granularities: ['word'],
    });

    return response.text;
  }
}
```

### Step 2: 하이브리드 검색 구현

```typescript
// backend/src/modules/ai/services/hybrid-search.service.ts
import { Injectable } from '@nestjs/common';

@Injectable()
export class HybridSearchService {
  async search(query: string, documents: any[]) {
    // 1. 벡터 검색
    const vectorResults = await this.vectorSearch(query);
    
    // 2. 키워드 검색 (BM25)
    const keywordResults = await this.keywordSearch(query);
    
    // 3. 하이브리드 스코어링
    const combined = this.combineResults(vectorResults, keywordResults);
    
    return combined;
  }
}
```

---

## 📝 논문 작성 팁

### 제목
"SyncNapse: [구체적 기술]를 활용한 [목적] 연구"

예:
- "SyncNapse: Whisper 기반 실시간 화자 분리를 통한 협업 학습 효과 연구"
- "하이브리드 검색 전략이 강의 노트 질의응답 시스템에 미치는 영향"

### 논문 구조
1. **Abstract**: 문제, 방법, 결과, 기여
2. **Introduction**: 배경, 동기, 연구 질문
3. **Related Work**: 기존 연구 조사
4. **Methodology**: 구현 상세, 실험 설계
5. **Results**: 정량적 결과, 그래프, 표
6. **Discussion**: 결과 해석, 한계점
7. **Conclusion**: 요약, 미래 연구

### 데이터 수집
- **정량적**: WER, MRR, 응답시간, 정확도
- **정성적**: 사용자 인터뷰, 설문조사
- **A/B 테스트**: 기존 vs 개선 비교

---

## 🎯 결론

SyncNapse는 이미 좋은 기반을 갖추고 있습니다. 제안된 개선 사항들을 단계적으로 구현하면:

1. **학술적 기여**: 교육 AI 분야의 실용적 연구
2. **기술적 혁신**: 최신 AI 기술 통합
3. **실용성**: 실제 교육 현장에서 검증 가능
4. **확장성**: 다른 도메인으로 확장 가능

**추천 시작점:**
1. Whisper 통합 (즉각적 효과)
2. 하이브리드 검색 (RAG 성능 향상)
3. 사용자 행동 로깅 (데이터 수집 시작)

이 세 가지만 구현해도 논문 1-2편은 충분히 작성 가능합니다!

---

**작성일**: 2025-12-07  
**버전**: 1.0  
**작성자**: AI Enhancement Research Team

